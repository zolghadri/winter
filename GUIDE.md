# راهنمای کامل پنل نوا پروکسی (⁦Nova Proxy⁩)

پنل نوا پروکسی یک پروکسی شخصی و رایگان است که روی ⁦Worker⁩ خودتان در ⁦Cloudflare⁩ اجرا می‌شود. هیچ سرور مشترکی وجود ندارد؛ هر کس پنل اختصاصی خودش را دارد و هیچ لاگی از ترافیک ذخیره نمی‌شود.

این راهنما شما را قدم‌به‌قدم از نصب تا مدیریت روزانه می‌برد. در انتها یک بخش «سوالات متداول» از ربات پشتیبانی تلگرام هم آمده است.

---

## پیش‌نیازها

- یک حساب رایگان ⁦Cloudflare⁩ (اگر ندارید: https://dash.cloudflare.com/sign-up ، یک دقیقه).
- (اختیاری) یک دامنه شخصی برای پایداری و سرعت بیشتر، مخصوصاً برای ⁦gRPC/XHTTP⁩ و تماس‌ها.

---

## گام ۱: نصب پنل

سه راهنمای امن برای نصب در https://novaproxy.online/setup/ قرار دارد. سایت Nova هیچ رمز Cloudflare یا API Token از شما دریافت نمی‌کند.

1. **نصب مستقیم Cloudflare (پیشنهادی):** دکمهٔ رسمی Deploy to Cloudflare منابع موردنیاز را داخل حساب خودتان می‌سازد. ورود و تأیید فقط روی دامنهٔ رسمی Cloudflare انجام می‌شود.
2. **ربات رسمی تلگرام:** ربات ⁦@IRNovaProxy_Bot⁩ مراحل نصب یا به‌روزرسانی را راهنمایی می‌کند. نام کاربری ربات را دقیقاً بررسی کنید.
3. **دستی با ⁦wrangler:⁩** برای کسانی که می‌خواهند خودشان از مخزن ⁦IRNova/Nova-Proxy⁩ دیپلوی کنند.

نکته امنیتی: رمز حساب Cloudflare یا API Token را هرگز در ⁦novaproxy.online⁩ وارد نکنید.

---

## گام ۲: اولین ورود و تعیین رمز ادمین

بعد از نصب، آدرس ⁦Worker⁩ خود را باز کنید؛ ویزارد نصب از شما می‌خواهد یک رمز ادمین بگذارید. این رمز برای ورود به پنل (`/admin`) لازم است. رمز قوی انتخاب کنید و در جای امن نگه دارید.

اگر «صفحه تست سرعت» باز شد یا وارد پنل نشدید: این صفحه عمداً به‌عنوان پوشش نمایش داده می‌شود. مستقیم به `/admin` (یا `/login`) بروید.

---

## گام ۳: نگاهی به داشبورد

بالای داشبورد چهار کارت می‌بینید: وضعیت شبکه، حجم دیتابیس، مصرف ⁦Worker⁩ (تعداد ⁦Request⁩ امروز از سقف روزانه ۱۰۰٬۰۰۰) و ترافیک امروز. کارت «⁦What's new⁩» تازه‌ترین قابلیت‌ها را نشان می‌دهد.

**منظور از ⁦Request:⁩** هر بار که یک کاربر یا برنامه درخواستی به ⁦Worker⁩ می‌فرستد، یک ⁦Request⁩ ثبت می‌شود. سقف رایگان روزانه ۱۰۰٬۰۰۰ است.

---

## گام ۴: تنظیمات پایه (حالت ایران)

ساده‌ترین راه: در داشبورد یک‌بار روی **⁦Iran mode⁩** بزنید. این حالت بهترین ترکیب مقاومت را یکجا فعال می‌کند: همه پروتکل‌ها + پخش پورت + ⁦Fragment + ECH⁩ (و ⁦QUIC⁩ را می‌بندد، ⁦IPv6⁩ را خاموش می‌کند).

برای تنظیم دستی، به بخش **⁦Settings⁩** بروید:
- **⁦Protocol:⁩** ⁦VLESS⁩ (پیشنهادی).
- **⁦Transport:⁩** ⁦WebSocket⁩ (سازگار با همه اپ‌ها).
- **⁦TLS fragment⁩ (عبور از ⁦DPI⁩):** یکی از پریست‌های ایران (⁦Gentle / Balanced / Aggressive⁩) را امتحان کنید؛ برای هر اپراتور (⁦MCI / Irancell⁩) ممکن است متفاوت باشد.

---

## گام ۵: گرفتن لینک اشتراک و اتصال اپ

در داشبورد، لینک اشتراک (⁦Subscription⁩) خود را کپی کنید یا ⁦QR⁩ را اسکن کنید. سپس **⁦Nova Client⁩** را نصب کنید، لینک را وارد کنید و وصل شوید. روی اندروید معمولاً «⁦Add to App⁩» یک‌کلیک اضافه می‌کند. اگر دستی به اپ‌های دیگر (مثل ⁦FlClash/mihomo⁩) اضافه می‌کنید، از همان لینک استفاده کنید.

**بهترین تنظیم برای همراه اول/ایرانسل:** پروتکل ⁦VLESS⁩، ترنسپورت ⁦WebSocket⁩، ⁦Fragment⁩ روشن (پریست مناسب شبکه‌تان)، و در صورت کندی از **⁦Nova Radar⁩** برای پیدا کردن ⁦IP⁩ تمیز استفاده کنید.

---

## گام ۶: افزودن کاربر (چند-کاربره)

در بخش **⁦Users⁩** می‌توانید برای هر نفر یک کاربر جدا با سهمیه حجم، تاریخ انقضا و تنظیمات اختصاصی بسازید. هر کاربر لینک اشتراک خودش را می‌گیرد و مصرف حجمش جداگانه شمرده می‌شود.

---

## گام ۷: ⁦IP⁩های تمیز و ⁦Nova Radar⁩

اگر شبکه کند شد یا یک ⁦IP⁩ بلاک شد، از **⁦Nova Radar⁩** استفاده کنید: داخل مرورگر سریع‌ترین ⁦IP⁩های ⁦Cloudflare⁩ را برای شبکهٔ فعلی پیدا می‌کند. بعد از اسکن، روی **⁦Apply⁩** بزنید تا ⁦IP⁩ها در بخش ⁦Clean IPs⁩ پنل ذخیره شوند و در اشتراک‌ها استفاده شوند. کاربران با یک‌بار ⁦Refresh⁩ کردن اشتراکشان آن‌ها را می‌گیرند.

بخش «⁦IP selection mode⁩» سه حالت دارد: ⁦Smart⁩ (بر اساس اپراتور)، ⁦Custom⁩ (لیست دستی)، ⁦Random.⁩ برای استفاده از ⁦IP⁩های ⁦Radar⁩، حالت ⁦Local/Custom-list⁩ مناسب است.

---

## گام ۸: ⁦WARP⁩ و تماس‌های صوتی/تصویری

بخش **⁦WireGuard/WARP⁩** برای دو کار است: (۱) تماس‌های ⁦FaceTime/WhatsApp/Telegram⁩ را از مسیر ⁦WARP⁩ رایگان عبور می‌دهد تا وصل شوند، در حالی که مرور وب روی پروکسی سریع می‌ماند. (۲) یک نود ⁦WireGuard/AmneziaWG⁩ می‌سازد.

اگر ⁦Endpoint⁩ پیش‌فرض فیلتر بود، از **منوی کشویی ⁦Endpoint⁩** یک آدرس تمیز انتخاب کنید (لیست به‌روز از پول اجتماعی خوانده می‌شود). پارامترهای ⁦Junk⁩ (⁦Jc/Jmin/Jmax⁩) هم برای ⁦AmneziaWG⁩ قابل تنظیم هستند.

---

## گام ۹: امنیت (مسیرهای مخفی و ۲⁦FA⁩)

- **⁦Disguise⁩ (مسیرهای مخفی):** پنل، لاگین و اشتراک را به یک آدرس خصوصی که فقط شما می‌دانید منتقل می‌کند تا کسی با حدس زدن `/admin` یا `/login` به آن‌ها نرسد (⁦Network⁩ & ⁦IPs⁩ → ⁦Disguise⁩). حتماً مسیرهای مخفی را یادداشت کنید.
- **۲⁦FA:⁩** با ⁦Google Authenticator/Authy⁩ یک کد ۶ رقمی هنگام ورود اضافه کنید (⁦Settings⁩ → ⁦Security⁩).
- **⁦Silent alerts:⁩** اگر نمی‌خواهید برای هر ورود/تغییر به بات پیام برود، این گزینه را روشن کنید.

---

## گام ۱۰: اتصال توکن ⁦Cloudflare +⁩ ربات تلگرام

در بخش **⁦Notifications⁩**:
- **توکن ⁦Cloudflare⁩ را وصل کنید** تا «مصرف ⁦Worker⁩» زنده نمایش داده شود و «به‌روزرسانی یک‌کلیکی» فعال شود. یک توکن با دسترسی ⁦Account Analytics: Read + Workers Scripts: Edit⁩ کافی است.
- **ربات تلگرام** را وصل کنید (⁦Bot token⁩ از ⁦BotFather + Chat ID⁩). با این کار می‌توانید پنل را از تلگرام مدیریت کنید و هشدارها را بگیرید. آموزش کامل: https://youtu.be/VPi6VEypJZ8

---

## گام ۱۱: به‌روزرسانی

اگر توکن ⁦Cloudflare⁩ را وصل کرده باشید، در بخش ⁦Notifications⁩ دکمهٔ **⁦Update now⁩** پنل را به آخرین نسخه می‌رساند (کد جایگزین می‌شود، تنظیمات و کاربران حفظ می‌شوند). روی دامنهٔ اختصاصی، اگر خطای «⁦Script name required⁩» دیدید، نام ⁦Worker⁩ خود را در فیلدی که ظاهر می‌شود وارد کنید.

---

## دامنهٔ اختصاصی

لازم نیست، ولی توصیه می‌شود. در ⁦Cloudflare⁩ → ⁦Worker⁩ شما → ⁦Settings⁩ → ⁦Domains⁩ & ⁦Routes⁩ → ⁦Add Custom Domain⁩، دامنه‌تان را به ⁦Worker⁩ وصل کنید، سپس همان را در پنل استفاده کنید. ⁦workers.dev⁩ در ایران فیلتر است؛ دامنهٔ اختصاصی پایدارتر است.

---

## عیب‌یابی سریع

- **پینگ خوب ولی وصل نمی‌شود / دانلود صفر:** ⁦Fragment⁩ را روشن/عوض کنید، یا از ⁦Nova Radar⁩ یک ⁦IP⁩ تمیز بگذارید.
- **قطع‌ووصل مکرر:** پروتکل/پورت دیگری امتحان کنید یا پریست ⁦Fragment⁩ را عوض کنید.
- **بعد از یک روز قطع می‌شود:** معمولاً ⁦IP⁩ تمیز نیاز به تازه‌سازی دارد؛ ⁦Radar⁩ بزنید و ⁦Apply⁩ کنید.
- **رمز پنل را فراموش کردم:** از «فراموشی رمز» با ورود یک‌کلیک تلگرام، یا سکرت ⁦ADMIN⁩ در ⁦Cloudflare⁩ استفاده کنید.

---

## سوالات متداول (FAQ)

_برگرفته از ربات پشتیبانی تلگرام نوا._

<details>
<summary><b>نوا پروکسی چیه؟</b></summary>

نوا پروکسی (Nova Proxy) یک پنل گرافیکی رایگان و قدرتمند مبتنی بر Cloudflare Workers هست که به شما اجازه می‌ده پروکسی شخصی (VLESS، Trojan، Shadowsocks و ...) بسازید.
تمام کارها روی حساب رایگان Cloudflare انجام می‌شه و نیازی به VPS یا پرداخت هزینه نیست.
لینک گیت‌هاب: https://github.com/IRNova/Nova-Proxy
سایت رسمی: https://novaproxy.online

</details>

<details>
<summary><b>منظور از Request چیست؟</b></summary>

هر بار که یک کاربر یا یک برنامه درخواستی به Worker ارسال می‌کند، یک Request ثبت می‌شود.

به زبان ساده، هر اتصال، هر بار باز کردن یک صفحه، هر فراخوانی API و بسیاری از فعالیت‌های مشابه می‌توانند یک Request ایجاد کنند.

Cloudflare میزان مصرف Worker را بر اساس تعداد Requestها محاسبه می‌کند.

</details>

<details>
<summary><b>گروه/تیم IRNova چیه و هدفش چیه؟</b></summary>

IRNova تیمی ایرانی هست که پروژه Nova Proxy رو توسعه داده. هدف اصلی تیم ارائه اینترنت آزاد و رایگان برای همه مردم ایران در شرایط فیلترینگ شدید هست.
تمام پروژه‌ها (پنل، NovaRadar، Nova Client و ...) متن‌باز و رایگان هستن و فقط با حمایت داوطلبانه کاربران ادامه پیدا می‌کنن.

</details>

<details>
<summary><b>Cloudflare چیه و مال کیه؟</b></summary>

Cloudflare یک شرکت آمریکایی بزرگه که خدمات CDN، DNS، امنیت و Workers (اجرای کد سمت سرور) ارائه می‌ده.
ما فقط از سرویس رایگان Workersش استفاده می‌کنیم و هیچ ارتباطی با فروش یا پشتیبانی پولی نداریم.

</details>

<details>
<summary><b>امنیت Nova Proxy چطوره؟</b></summary>

هیچ لاگی از ترافیک شما ذخیره نمی‌شه (پروکسی شخصی شماست).
ارتباط با TLS رمزنگاری می‌شه.
امکان احراز هویت دو مرحله‌ای (2FA) وجود داره.
تمام کد پروژه متن‌باز و روی گیت‌هاب در دسترسه.

</details>

<details>
<summary><b>پنل کجا نصب می‌شه؟</b></summary>

نل روی Worker شخصی خود شما در حساب Cloudflare نصب می‌شه.
هیچ سرور مشترکی وجود نداره: هر نفر پنل اختصاصی خودش رو داره.

</details>

<details>
<summary><b>چطور پنل بسازم؟</b></summary>

راهنمای رسمی نصب و به‌روزرسانی:
🔗 https://novaproxy.online/setup/
مراحل:

اکانت Cloudflare بساز (رایگان)
روش نصب امن را انتخاب کن
بعد از نصب، آدرس Worker را باز کن و رمز پنل را بساز

آموزش کامل ویدیو:
https://youtu.be/NGywxMUPtio (قسمت اول)

</details>

<details>
<summary><b>نیاز به دامنه اختصاصی دارم؟</b></summary>

خیر. می‌تونی با آدرس workers.dev شروع کنی.
اما داشتن دامنه شخصی سرعت و پایداری رو خیلی بیشتر می‌کنه (به خصوص برای gRPC و XHTTP).
آموزش اتصال دامنه: https://youtu.be/liu9j1yf_8o

</details>

<details>
<summary><b>ربات تلگرام پنل رو چطور وصل کنم؟</b></summary>

آموزش کامل: https://youtu.be/VPi6VEypJZ8
شامل:

ساخت API Token کلادفلر
ساخت ربات با BotFather
اتصال Chat ID

</details>

<details>
<summary><b>بهترین تنظیمات برای همراه اول/ایرانسل چیه؟</b></summary>

پیشنهاد فعلی:

پروتکل: VLESS
Transport: WebSocket
TLS Fingerprint: Random

برای جزئیات بیشتر ویدیو تنظیمات رو ببین: https://youtu.be/TzGvgBtNTG0

</details>

<details>
<summary><b>لینک‌های آموزشی مهم</b></summary>

نصب از صفر: https://youtu.be/NGywxMUPtio
تنظیمات کامل پنل: https://youtu.be/TzGvgBtNTG0
اتصال دامنه: https://youtu.be/liu9j1yf_8o
API + ربات تلگرام: https://youtu.be/VPi6VEypJZ8

</details>

<details>
<summary><b>مشکل دارم، چطور کمک بگیرم؟</b></summary>

اول آموزش‌های یوتیوب رو چک کن.
اگر حل نشد در گروه تلگرام بپرس: https://t.me/irnovaproxy_group
در نهایت میتونی کل پیغامت رو از طریق همین ربات برامون ارسال کنی و تیم ما همیشه پاسخ شمارو میده

</details>

<details>
<summary><b>چطور دیتابیس D1 رو حذف کنم؟</b></summary>

برای حذف کامل دیتابیس D1 در Nova Proxy مراحل زیر را به ترتیب انجام دهید:

وارد داشبورد Cloudflare شوید: dash.cloudflare.com
از منوی سمت چپ روی Storage & Databases کلیک کنید.
روی D1 کلیک کنید.
دیتابیسی که برای Nova Proxy ساخته‌اید را پیدا کنید (معمولاً نامی مثل nova-db یا مشابه دارد).
روی سه‌نقطه (⋯) کنار نام دیتابیس کلیک کنید.
گزینه Delete را انتخاب کنید.
نام دیتابیس را دقیقاً تایپ کنید تا تأیید شود و روی Delete database بزنید.


بعد از حذف D1:

Worker شما ممکن است با خطا مواجه شود (چون Binding به D1 قطع شده).
پیشنهاد می‌شود Worker را هم حذف کنید و یک نصب جدید انجام دهید (از طریق https://novaproxy.online/setup/).
میتونید از همین ربات هم نصب رو انجام بدین

</details>

<details>
<summary><b>موقع ساخت پنل می‌گوید اکانت را وریفای کنید</b></summary>

باید اکانت Cloudflare خود را با ایمیل واقعی (ترجیحاً Gmail) وریفای کنید.
ایمیل‌های یکبارمصرف (مثل Atomic Mail) گاهی باعث این خطا می‌شوند. یک اکانت جدید با Gmail بسازید و دوباره امتحان کنید.
سعی کنید از گزینه ایجاد با حساب گوگل استفاده کنید در غیر این صورت یکبار وارد کلودفلر بشین تا صفحه حساب تایید نیست ظاهر بشه و بعد زدن وریفای ایمیلی از سمت کلودفلر برای شما ارسال میشه بعد تایید اون حسابتون وریفای میشه

</details>

<details>
<summary><b>بعد از نصب، صفحه تست سرعت باز می‌شود یا وارد پنل نمی‌شوم</b></summary>

این یک صفحه استتار هست 
به انتهای آدرس Worker خود عبارت /admin اضافه کنید.
مثال:
https://your-worker.workers.dev/admin

</details>

<details>
<summary><b>کانفیگ پینگ خوب می‌دهد اما وصل نمی‌شود / دانلود و آپلود صفر است</b></summary>

در پنل بروید به تنظیمات → استتار اثر انگشت (TLS Fingerprint) را روی Random بگذارید.
همچنین تعداد IPهای تمیز را روی ۵۰۰ بگذارید و حالت را روی Random تنظیم کنید.

</details>

<details>
<summary><b>کانفیگ‌ها هی قطع و وصل می‌شوند</b></summary>

Fingerprint را روی Random بگذارید.
از Transport WebSocket استفاده کنید.
تعداد IPها را کم کنید (مثلاً ۱۰-۵۰ تا).
اگر مشکل ادامه داشت، پروتکل را به Trojan تغییر دهید.

</details>

<details>
<summary><b>بعد از یک روز کانفیگ‌ها قطع می‌شوند</b></summary>

احتمالاً به محدودیت روزانه Request Cloudflare (۱۰۰ هزار درخواست) رسیده‌اید.
راه‌حل:

یک اکانت Cloudflare جدید بسازید و پنل را دوباره نصب کنید.
یا تعداد کاربران/مصرف را محدود کنید.

</details>

<details>
<summary><b>اپ Nova Client روی اندروید وصل نمی‌شود</b></summary>

نسخه فعلی اپ اندروید هنوز در حال رفع باگ است. فعلاً از Karing استفاده کنید و اشتراک پنل را داخل آن ایمپورت کنید.

</details>

<details>
<summary><b>اپ Nova Client روی iOS نصب می‌شود؟</b></summary>

بله، نسخه iOS در حال انتشار در اپ استور است. فعلاً می‌توانید از مرورگر یا Karing استفاده کنید.

</details>

<details>
<summary><b>در زمان قطعی اینترنت (نت ملی) Nova Proxy کار می‌کند؟</b></summary>

بله، در زمان قطعی اینترنت ملی، Nova Proxy معمولاً کار می‌کند چون از زیرساخت خارجی Cloudflare استفاده می‌کند. اما ممکن است در برخی اپراتورها سرعت کمتر باشد.
داریم شرایط رو اوکی میکنیم ولی اگه ای پی های سفید رو ایران ببنده راهی دیگه پیدا میکنیم

</details>

<details>
<summary><b>چطور دامنه اختصاصی رو به Nova Proxy متصل کنیم؟</b></summary>

برای اتصال دامنه به Nova Proxy، آموزش کامل و مرحله‌به‌مرحله در ویدیوی زیر قرار داده شده است:

🎥 https://youtu.be/liu9j1yf_8o

در این آموزش موارد زیر توضیح داده شده:

• دریافت دامنه رایگان یا خرید دامنه
• اضافه کردن دامنه به Cloudflare
• تنظیم Name Server ها
• اتصال دامنه به Worker
• ساخت Subdomain
• ورود به پنل از طریق دامنه اختصاصی

#NovaProxy
#Domain
#Cloudflare
#Tutorial

</details>

<details>
<summary><b>از کجا بفهمم چقدر از سهمیه Request روزانه Cloudflare مصرف شده است؟</b></summary>

برای مشاهده میزان مصرف Request دو روش وجود دارد.

روش اول: داخل Cloudflare

وارد حساب Cloudflare شوید.

سپس از منوی سمت چپ وارد بخش Workers & Pages شوید.

در همین صفحه و در سمت راست، آمار مربوط به مصرف Worker نمایش داده می‌شود.

در این قسمت می‌توانید تعداد Requestهای مصرف‌شده و میزان باقی‌مانده از سهمیه روزانه اکانت خود را مشاهده کنید.

توجه داشته باشید که این آمار مربوط به کل اکانت Cloudflare است و فقط به یک Worker خاص محدود نمی‌شود.

روش دوم: داخل پنل Nova Proxy

در نسخه‌های جدید Nova Proxy می‌توانید میزان مصرف Requestها را مستقیماً از داخل پنل مشاهده کنید.

برای این قابلیت باید ابتدا Cloudflare API را به پنل متصل کنید.

پس از اتصال API، اطلاعات مصرف Requestها در بخش «درخواست‌های ورکر» نمایش داده می‌شود.

🎥 آموزش اتصال Cloudflare API:

https://youtu.be/

(لینک ویدیوی آموزش API را اینجا قرار دهید)

</details>

<details>
<summary><b>مزیت اتصال Cloudflare API و راه‌اندازی ربات تلگرام چیست؟</b></summary>

اتصال Cloudflare API و ربات تلگرام اجباری نیست و Nova Proxy بدون آن‌ها هم به‌خوبی کار می‌کند.

اما با فعال کردن این قابلیت‌ها امکانات بیشتری در اختیار شما قرار می‌گیرد.

✅ مشاهده میزان مصرف Requestهای Cloudflare از داخل پنل

✅ مشاهده تعداد Requestهای باقی‌مانده بدون نیاز به ورود به داشبورد Cloudflare

✅ دریافت اطلاعات Worker به‌صورت مستقیم از حساب Cloudflare

✅ دریافت اعلان ورود به پنل مدیریت در تلگرام

✅ دریافت اعلان استفاده از لینک Subscription توسط کاربران

✅ دسترسی سریع‌تر به وضعیت سرویس از طریق ربات تلگرام

✅ مدیریت آسان‌تر و نظارت بیشتر روی پنل

اگر فقط از Nova Proxy برای استفاده شخصی استفاده می‌کنید، اتصال API و ربات تلگرام ضروری نیست.

اما اگر چند کاربر دارید یا می‌خواهید وضعیت پنل و مصرف Cloudflare را راحت‌تر مدیریت کنید، فعال‌سازی این بخش‌ها توصیه می‌شود.

</details>

<details>
<summary><b>بخش Proxy / CDN چیست و چه کاربردی دارد؟</b></summary>

بخش Proxy / CDN به شما امکان می‌دهد برای دسترسی به سرویس‌هایی که پشت Cloudflare قرار دارند از سرورهای واسط (Proxy IP یا SOCKS5) استفاده کنید.

این قابلیت باعث می‌شود سایت‌هایی مانند Twitter، ChatGPT و برخی سرویس‌های مشابه که دسترسی مستقیم به آن‌ها از طریق Cloudflare Workers امکان‌پذیر نیست، قابل استفاده شوند.

#NovaProxy #Cloudflare #ProxyIP #SOCKS5


---

❓ Proxy IP و SOCKS5 چه تفاوتی دارند؟

هر دو نقش واسط ارتباطی را دارند اما SOCKS5 معمولاً سازگاری و پایداری بیشتری دارد و برای اکثر کاربران انتخاب بهتری محسوب می‌شود.

در Nova Proxy پیشنهاد می‌شود از حالت‌های Proxy IP یا SOCKS5 استفاده کنید.

#NovaProxy #SOCKS5 #ProxyIP #Cloudflare


---

❓ آیا استفاده از بخش Proxy / CDN اجباری است؟

خیر.

اگر فقط از سایت‌ها و سرویس‌های معمولی استفاده می‌کنید، نیازی به فعال‌سازی این بخش ندارید.

این قابلیت بیشتر برای دسترسی به سرویس‌هایی کاربرد دارد که پشت Cloudflare قرار دارند و بدون پروکسی قابل دسترس نیستند.

#NovaProxy #CloudflareWorkers #ProxyIP #CDN


---

❓ آیا Proxy / CDN باعث می‌شود IP ثابت داشته باشم؟

خیر.

این بخش برای ارائه IP ثابت طراحی نشده است.

پروکسی‌های موجود ممکن است در آینده تغییر کنند، از دسترس خارج شوند یا جایگزین شوند.

اگر به IP ثابت نیاز دارید، باید از راهکارهای اختصاصی دیگر استفاده کنید.

#NovaProxy #ProxyIP #SOCKS5 #StaticIP


---

❓ آیا استفاده از Proxy / CDN برای صرافی‌ها مناسب است؟

خیر.

به دلیل تغییرپذیر بودن IPها، استفاده از این قابلیت برای صرافی‌ها، حساب‌های مالی، پرداخت‌های بین‌المللی و سرویس‌هایی که روی IP حساس هستند توصیه نمی‌شود.

#NovaProxy #ProxyIP #SOCKS5 #Exchange


---

❓ بعد از انتخاب پروکسی چرا تغییری مشاهده نمی‌کنم؟

بعد از ذخیره تنظیمات Proxy / CDN باید لینک Subscription خود را مجدداً آپدیت کنید.

تا زمانی که Subscription در برنامه کلاینت به‌روزرسانی نشود، تنظیمات جدید اعمال نخواهد شد.

#NovaProxy #Subscription #ProxyIP #Cloudflare


---

❓ کدام کشور را انتخاب کنیم؟

بهترین کشور برای هر کاربر متفاوت است و به اپراتور، موقعیت جغرافیایی و کیفیت پروکسی بستگی دارد.

پیشنهاد می‌شود چند کشور مختلف را تست کنید و گزینه‌ای را انتخاب کنید که کمترین تأخیر و بیشترین پایداری را دارد.

#NovaProxy #ProxyIP #SOCKS5 #Cloudflare


---

❓ چرا بعد از دریافت فهرست، تعداد زیادی پروکسی نمایش داده می‌شود؟

Nova Proxy فهرست پروکسی‌ها را از منابع مختلف دریافت می‌کند و آن‌ها را بر اساس کشور دسته‌بندی می‌کند.

برای مثال ممکن است ۳۰۰ پروکسی در ۳۰ کشور مختلف در دسترس باشد که می‌توانید هر کدام را انتخاب و استفاده کنید.

#NovaProxy #ProxyIP #SOCKS5 #CDN


---

❓ اگر پروکسی انتخاب‌شده کار نکند چه باید کرد؟

کافی است دوباره وارد بخش Proxy / CDN شوید و یک IP دیگر یا یک کشور دیگر را انتخاب کنید.

پروکسی‌ها عمومی هستند و ممکن است در طول زمان از دسترس خارج شوند.

#NovaProxy #ProxyIP #SOCKS5 #Troubleshooting


---

❓ آیا فعال کردن Proxy / CDN روی همه سایت‌ها تأثیر می‌گذارد؟

خیر.

این قابلیت عمدتاً زمانی استفاده می‌شود که مقصد نهایی پشت Cloudflare باشد.

برای سایر سایت‌ها و سرویس‌ها معمولاً مسیر پیش‌فرض Nova Proxy استفاده می‌شود.

</details>

<details>
<summary><b>برای استفاده از Nova Proxy نیاز به دامنه دارم؟</b></summary>

خیر.

برای راه‌اندازی اولیه فقط به یک اکانت Cloudflare نیاز دارید و می‌توانید از آدرس workers.dev استفاده کنید.

</details>

<details>
<summary><b>چطور دامنه اختصاصی رو به Nova Proxy متصل کنیم؟</b></summary>

برای اتصال دامنه به Nova Proxy، آموزش کامل و مرحله‌به‌مرحله در ویدیوی زیر قرار داده شده است:

🎥 https://youtu.be/liu9j1yf_8o

در این آموزش موارد زیر توضیح داده شده:

• دریافت دامنه رایگان یا خرید دامنه
• اضافه کردن دامنه به Cloudflare
• تنظیم Name Server ها
• اتصال دامنه به Worker
• ساخت Subdomain
• ورود به پنل از طریق دامنه اختصاصی

</details>

<details>
<summary><b>در بخش شبکه و آیپی، «تست اتصال» اینستاگرام را «مسدود» نشان می‌دهد. مشکل از چیست؟</b></summary>

این تست از مرورگری که پنل را در آن باز کرده‌اید انجام می‌شود، نه از طریق نوا. یعنی فقط نشان می‌دهد همان مرورگر به آن سایت‌ها دسترسی دارد یا نه و ترافیک تست اصلا از پروکسی شما رد نمی‌شود.

پس اگر پنل را با اینترنت ایران و بدون فیلترشکن باز کرده باشید، اینستاگرام (و معمولا یوتیوب و ایکس) «مسدود» نشان داده می‌شوند، حتی وقتی پنل شما کاملا سالم است. این یعنی «این مرورگر به اینستاگرام نمی‌رسد»، نه «پنل شما خراب است».

ملاک درست: لینک اشتراک را در اپ کلاینت وارد کنید، اتصال را وصل کنید و اینستاگرام را داخل گوشی باز کنید. اگر باز شد، پنل شما سالم است.

نکته: تست اینستاگرام از بقیه حساس‌تر است و گاهی حتی با اتصال سالم هم «مسدود» نشان می‌دهد، چون تست فقط یک تصویر کوچک از سایت را ۶ ثانیه امتحان می‌کند و هر خطایی را «مسدود» می‌شمارد. ردیف‌های Google و Cloudflare معیار بهتری برای سالم بودن دسترسی مرورگرتان هستند.

</details>

<details>
<summary><b>چطور کشور/لوکیشن آی‌پی خروجی را انتخاب کنم؟</b></summary>

این قابلیت حذف نشده؛ فقط جایش عوض شده و حالا داخل تنظیمات هر کاربر است.

<b>روش (سمت ادمین):</b> در پنل به «کاربران» بروید، کاربر موردنظر را ویرایش کنید، بخش «⁦Advanced options⁩» را باز کنید و در فیلد «⁦Proxy IATA⁩» کد سه‌حرفیِ شهر مقصد را بنویسید. چند نمونه: ⁦fra⁩ آلمان، ⁦cdg⁩ فرانسه، ⁦ams⁩ هلند، ⁦lhr⁩ انگلیس، ⁦sjc⁩ آمریکا، ⁦sin⁩ سنگاپور. بعد از ذخیره، کانفیگ‌های همان کاربر از آن دیتاسنتر خارج می‌شوند.

<b>آی‌پی تمیزِ متناسب با اپراتور:</b> اگر منظورتان آی‌پی تمیزِ هماهنگ با اپراتور ایرانی است (نه کشور خروجی)، در همان بخش از «⁦IP Operator⁩» اپراتور را انتخاب کنید (ایرانسل، همراه اول، رایتل، شاتل).

نکته: کد ⁦IATA⁩ همان کد فرودگاه شهر است و نزدیک‌ترین دیتاسنتر ⁦Cloudflare⁩ انتخاب می‌شود؛ در دسترس بودن هر لوکیشن به مسیر شبکه بستگی دارد.

</details>

<details>
<summary><b>رمز ورود به پنل را فراموش کرده‌ام، چطور وارد شوم؟</b></summary>

چند راه دارید. از بالا به پایین امتحان کنید:

<b>۱. اگر ربات تلگرام پنل را وصل کرده‌اید (سریع‌ترین راه):</b>
به ربات خودتان <code>/start</code> بفرستید. اولین دکمه، لینک ورود یک‌کلیکی به پنل است و اصلا به رمز نیاز ندارد.

<b>۲. از صفحه ورود:</b>
در صفحه <code>/login</code> روی «رمز را فراموش کردید؟» بزنید، بعد «ارسال لینک ورود از طریق تلگرام». لینک به چت ادمین خود پنل می‌رود (نه به کسی که دکمه را زده)، حدود ۵ تا ۱۰ دقیقه اعتبار دارد و اگر منقضی شد دوباره بزنید. حداکثر ۳ بار در ۱۰ دقیقه.

<b>۳. اگر تلگرام وصل نیست، با توکن Cloudflare:</b>
در همان بخش، یک Cloudflare API Token از همان اکانتی که ورکر رویش است وارد کنید و «بازیابی با توکن Cloudflare» را بزنید. یک رمز جدید ۱۶ کاراکتری به شما نشان داده می‌شود.

<b>۴. از داشبورد Cloudflare (آخرین راه):</b>
وارد داشبورد شوید، به KV بروید و کلید <code>admin_pass</code> را باز کنید. رمز همان‌جا به صورت متن ساده ذخیره شده؛ می‌توانید بخوانید یا عوضش کنید. اگر پنل روی D1 است، همین مقدار در جدول <code>kvstore</code> است. راه دیگر: سکرت <code>ADMIN</code> را در Settings → Variables and Secrets ورکر ست کنید. بعد از تغییر تا یک دقیقه صبر کنید تا کش پنل تازه شود.

اگر ۲FA هم روشن است و به authenticator دسترسی ندارید، فقط راه ۱ و ۲ کار می‌کند؛ در غیر این صورت باید کلید <code>admin_2fa.json</code> را از KV حذف کنید.

توجه: دکمه «تنظیم رمز ادمین» و صفحه <code>/install</code> فقط روی پنل تازه و تنظیم‌نشده کار می‌کنند و برای پنلی که از قبل رمز دارد خطا می‌دهند.

</details>
