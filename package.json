{
  "name": "winter",
  "version": "4.6.5",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:debug": "NOVA_BUILD_MODE=debug node scripts/build.mjs",
    "check": "npm run build && node --check worker.js && wrangler deploy --dry-run --outdir .wrangler/dry-run",
    "test": "npm run build && node --test",
    "deploy": "npm run build && wrangler deploy"
  },
  "cloudflare": {
    "bindings": {
      "KV": {
        "description": "Nova's compatibility and migration store. Cloudflare creates and binds it automatically."
      },
      "DB": {
        "description": "Nova's primary D1 database for settings, users, usage, logs, and authentication."
      },
      "NOVA_CLAIM_TOKEN": {
        "description": "Optional but recommended. A random string that ties first-time setup to you: with it set, the panel only accepts its first admin password at /install?claim=<value>, so nobody who finds the address before you can claim the panel. Generate one with `openssl rand -hex 16` and keep a copy. Leave blank to keep the old behaviour, where whoever opens /install first sets the password. You can change or remove it later in Settings, Variables and Secrets."
      }
    }
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "esbuild": "^0.28.1",
    "html-minifier-terser": "^7.2.0",
    "wrangler": "^4.116.0"
  }
}
